<Window x:Class="GeniaProxy.ImportProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт профиля GeniaProxy 4.2.1"
        Width="690"
        Height="470"
        MinWidth="590"
        MinHeight="410"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#071521"
        Foreground="#F2F7FA"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#06131D" />
            <Setter Property="Foreground" Value="#F2F7FA" />
            <Setter Property="BorderBrush" Value="#315365" />
            <Setter Property="CaretBrush" Value="#28D9C5" />
            <Setter Property="Padding" Value="9,6" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="105" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Padding" Value="13,6" />
            <Setter Property="Foreground" Value="#EFFFFD" />
            <Setter Property="Background" Value="#0B2530" />
            <Setter Property="BorderBrush" Value="#28D9C5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#123744" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#18505B" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Название профиля"
                   Foreground="#9CADB8" />
        <TextBox x:Name="ProfileNameTextBox"
                 Grid.Row="1"
                 Margin="0,6,0,13"
                 Text="Новый профиль"
                 MaxLength="100" />

        <TextBlock Grid.Row="2"
                   Text="Ссылка Hysteria2, VLESS + XHTTP + TLS/REALITY или конфигурация JSON"
                   Foreground="#9CADB8" />
        <TextBox x:Name="SourceTextBox"
                 Grid.Row="3"
                 Margin="0,6,0,14"
                 AcceptsReturn="True"
                 AcceptsTab="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12" />

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Content="Загрузить JSON..."
                    HorizontalAlignment="Left"
                    Margin="0"
                    Click="LoadJsonButton_Click" />

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Отмена"
                        IsCancel="True" />
                <Button Content="Импортировать"
                        IsDefault="True"
                        Click="ImportButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>

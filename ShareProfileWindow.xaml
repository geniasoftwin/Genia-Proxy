<Window x:Class="GeniaProxy.ShareProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Поделиться профилем GeniaProxy"
        Width="560"
        Height="690"
        MinWidth="500"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#071521"
        Foreground="#F2F7FA"
        FontFamily="Segoe UI"
        UseLayoutRounding="True">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="115" />
            <Setter Property="MinHeight" Value="34" />
            <Setter Property="Margin" Value="8,0,0,0" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Foreground" Value="#EFFFFD" />
            <Setter Property="Background" Value="#0B2530" />
            <Setter Property="BorderBrush" Value="#28D9C5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#123744" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#18505B" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProfileTitleText"
                   FontSize="20"
                   FontWeight="SemiBold" />

        <Border Grid.Row="1"
                Margin="0,12,0,14"
                Padding="11"
                Background="#38251A"
                BorderBrush="#B66B35"
                BorderThickness="1"
                CornerRadius="0">
            <TextBlock Text="QR содержит стандартную мобильную ссылку подключения с адресом сервера и секретом. Показывайте его только доверенному получателю."
                       TextWrapping="Wrap"
                       Foreground="#FFD7B8" />
        </Border>

        <Border Grid.Row="2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="White"
                Padding="12"
                CornerRadius="0">
            <Image x:Name="QrImage"
                   Width="410"
                   Height="410"
                   Stretch="Uniform"
                   RenderOptions.EdgeMode="Aliased" />
        </Border>

        <TextBlock x:Name="PayloadInformationText"
                   Grid.Row="3"
                   HorizontalAlignment="Center"
                   Foreground="#9CADB8"
                   Margin="0,12,0,14" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Копировать ссылку"
                    Click="CopyLinkButton_Click" />
            <Button Content="Копировать QR"
                    Click="CopyQrButton_Click" />
            <Button Content="Сохранить PNG..."
                    Click="SaveQrButton_Click" />
            <Button Content="Закрыть"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
